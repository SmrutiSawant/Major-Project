React Frontend
----------------
This folder assumes your existing FloodPreventer React app.

Change required:
- Replace rule-based flood logic with POST call to:
  http://localhost:8000/predict

See backend API schema for request/response.
